package org.yearup.configurations;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DatabaseConfig {
    // Using Spring Boot auto-configured DataSource via spring.datasource.* properties.
}